<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Entrant Name -->
    <TextView
        android:id="@+id/entrantName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Entrant Name"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Entrant Email -->
    <TextView
        android:id="@+id/entrantEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Entrant Email"
        android:textSize="16sp" />
    <!-- Optional Phone Number -->
    <TextView
        android:id="@+id/entrantPhoneNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Phone Number"
        android:textSize="16sp"
        android:visibility="gone"
        android:paddingTop="8dp" /> <!-- Optional padding for spacing --> <!-- Hide by default -->
    <TextView
        android:id="@+id/profileImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Entrant Profile Image"
        android:textSize="16sp" />

</LinearLayout>
